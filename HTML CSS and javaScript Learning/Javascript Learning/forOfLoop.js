//For Of Loop works same as For loop and it is used to iterate the array

const fruits=["Apple","Banana","Kiwi","Orange"];

// const fruits2=[];  

for(let fruit of fruits)
{
    // fruits2.push(fruit.toUpperCase()); //if you want to push in new Array
    console.log(fruit.toUpperCase());
}